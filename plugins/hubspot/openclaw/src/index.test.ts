import { describe, expect, it } from "vitest"; import entry from "./index.js"; import { getToolPluginMetadata } from "openclaw/plugin-sdk/tool-plugin";
describe("hubspot plugin", () => { it("declares the complete named surface", () => { expect(getToolPluginMetadata(entry)?.id).toBe("arcforgelabs-hubspot"); expect(getToolPluginMetadata(entry)?.tools.map((x) => x.name)).toEqual(["hs_status","hs_contacts","hs_companies","hs_deals","hs_tickets","hs_notes","hs_tasks","hs_emails","hs_calls","hs_meetings","hs_associations","hs_pipelines","hs_properties","hs_owners","hs_lists","hs_workflows","hs_objects"]); }); });
